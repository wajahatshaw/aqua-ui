import React,{Componet} from 'react-native';
import {ThemeProvider} from 'react-native-elements'

class ThemeProvider extends Component{
    constructor(props){
        super(props)
    }

    render(){
        return(
            <View style={{flex:1}}>
            </View>
        )
    }
}
